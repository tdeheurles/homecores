sudo: required

services:
  - docker

before_install:
  - docker pull ubuntu:latest

script:
  - docker run -d ubuntu:latest /bin/bash -c "while true; do sleep 5; done" > container_id
  - docker ps -a
  - cat container_id
  - docker exec `cat container_id` ls
